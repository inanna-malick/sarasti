import React from 'react';

/**
 * Click face → side panel with sparkline, binding params, family context.
 */
export function DetailPanel(): React.ReactElement {
  throw new Error('Not implemented — see interaction/detail worktree');
}

/**
 * Mouse hover → face picking → highlight + tooltip.
 */
export function setupHoverInteraction(
  _container: HTMLElement,
): { dispose: () => void } {
  throw new Error('Not implemented — see interaction/hover worktree');
}
